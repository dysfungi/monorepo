apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-cert-api-frank-sh
spec:
  commonName: api.frank.sh
  dnsNames:
    - api.frank.sh
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  secretName: api-frank-sh-tls
