# shellcheck disable=SC2034,SC2155
export AWS_ACCESS_KEY_ID="$(op item get 'S3 Credentials (frankenstorage)' --fields=username)"
export AWS_SECRET_ACCESS_KEY="$(op item get 'S3 Credentials (frankenstorage)' --fields=password --reveal)"
export VULTR_API_KEY="$(op item get 'Vultr API Key' --fields=password --reveal)"
